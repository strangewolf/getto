# Frontend-only POC: no environment variables are required.
# Copy this file to web/.env only if you want local overrides.
#
# Optional:
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
#
# Location map: Leaflet + OSM (no API key).
